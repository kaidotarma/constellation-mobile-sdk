# Contracts: [FEATURE NAME]

Store feature-specific API, JavaScript bridge, serialized payload, or resource contracts here when the feature crosses a module or process boundary.

Each contract should document:

- producer and consumer;
- request or input shape;
- response or output shape;
- errors and security constraints;
- compatibility and versioning expectations;
- an example that contains no real customer data or credentials.
